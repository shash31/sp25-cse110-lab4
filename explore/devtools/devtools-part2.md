1. The bug was that the function was adding the two input variables as strings instead of numbers. Thus, it just concatenated them instead of adding them up as numbers.
2. I would fix it by explicitly changing them to number types and then adding them up in the calculateSum() function. 
3. 